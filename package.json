{
  "name": "final-postes-shop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install npm-run-all && npm-run-all install:*",
    "install:server": "npm install",
    "install:client": "cd client && npm install",
    "start": "npm-run-all -p -r start:*",
    "start:server": "nodemon server.js",
    "start:client": "cd client && npm run start",
    "build": "cd client && npm install && npm run build"
  },
  "author": "Wojciech Borkowski",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "mongodb": "^4.8.0",
    "mongoose": "^6.4.4",
    "nodemon": "^2.0.19"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
